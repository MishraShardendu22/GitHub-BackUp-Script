SMTP_PORT=
SMTP_HOST=

SMTP_TO=
SMTP_FROM=

SMTP_PASSWORD=
SMTP_USERNAME=

# OpenRouter — API key is required; model is a fallback default
# (models are fetched dynamically from OpenRouter)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/auto